# 绿源农业项目部署完整教程

## 目录
1. [环境准备](#1-环境准备)
2. [启动后端服务](#2-启动后端服务)
3. [前端配置与测试](#3-前端配置与测试)
4. [项目打包](#4-项目打包)
5. [Git 仓库管理](#5-git-仓库管理)
6. [服务器部署](#6-服务器部署)

---

## 1. 环境准备

### 1.1 安装必要软件

```bash
# 1. 安装 Node.js (14+ 版本)
# 官网下载: https://nodejs.org/
node --version  # 检查版本

# 2. 安装 MongoDB
# 官网下载: https://www.mongodb.com/try/download/community
# 或使用 Docker:
docker run -d -p 27017:27017 --name mongodb mongo:latest

# 3. 安装 Git
# 官网下载: https://git-scm.com/
git --version  # 检查版本
```

### 1.2 项目结构确认

```
lvyuan-agriculture/
├── backend/          # 后端项目
├── css/              # 前端样式
├── js/               # 前端脚本
├── images/           # 图片资源
├── index.html        # 主页面
└── start-server.sh   # 启动脚本
```

---

## 2. 启动后端服务

### 2.1 安装依赖

```bash
cd backend
npm install
```

### 2.2 配置环境变量

编辑 `backend/.env` 文件：

```env
PORT=3000
MONGODB_URI=mongodb://localhost:27017/lvyuan_agriculture
JWT_SECRET=your-super-secret-key-change-this
JWT_EXPIRE=7d
NODE_ENV=development
```

### 2.3 初始化数据库

```bash
cd backend
node seed.js
```

输出示例：
```
数据库连接成功
已清空商品数据
已插入 22 个商品
管理员账号已创建: admin@lvyuan.com / admin123
测试用户已创建: test@example.com / test123
数据初始化完成！
```

### 2.4 启动服务器

```bash
# 开发模式（推荐）
npm run dev

# 或生产模式
npm start
```

看到以下输出表示成功：
```
MongoDB 连接成功
服务器运行在端口 3000
API地址: http://localhost:3000/api
```

---

## 3. 前端配置与测试

### 3.1 配置 API 地址

编辑 `js/api.js`：

```javascript
// 开发环境
const API_BASE_URL = 'http://localhost:3000/api';

// 生产环境（部署后修改）
// const API_BASE_URL = 'https://your-domain.com/api';
```

### 3.2 测试 API 接口

打开浏览器访问：
- 健康检查: http://localhost:3000/api/health
- 商品列表: http://localhost:3000/api/products

### 3.3 前端页面测试

1. 打开 `index.html`（使用 Live Server 或直接在浏览器打开）
2. 点击右上角用户图标登录
3. 使用测试账号: `test@example.com` / `test123`
4. 添加商品到购物车，查看是否同步到后端

---

## 4. 项目打包

### 4.1 前端打包（纯 HTML 无需打包）

如果是纯 HTML/CSS/JS 项目，直接复制以下文件到 `dist` 文件夹：

```bash
mkdir -p dist
cp -r css js images index.html dist/
```

### 4.2 后端打包

```bash
cd backend
# 安装生产依赖
npm install --production

# 或使用 Docker
docker build -t lvyuan-backend .
```

---

## 5. Git 仓库管理

### 5.1 初始化仓库

```bash
# 在项目根目录
git init

# 创建 .gitignore
cat > .gitignore << 'EOF'
# Dependencies
backend/node_modules/
node_modules/

# Environment
backend/.env
.env

# Logs
*.log
npm-debug.log*

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/
EOF
```

### 5.2 提交代码

```bash
# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 绿源农业电商平台 v1.0

- 完整前端页面（首页、产品、关于、案例、联系）
- Express + MongoDB 后端 API
- 用户认证（JWT）
- 购物车、订单、评论、订阅功能
- 响应式设计，支持中英文切换"
```

### 5.3 上传到 GitHub/Gitee

```bash
# 创建远程仓库（在 GitHub/Gitee 网页上创建）

# 添加远程地址
git remote add origin https://github.com/yourusername/lvyuan-agriculture.git

# 推送代码
git push -u origin main
```

---

## 6. 服务器部署

### 6.1 使用 Vercel/Netlify 部署前端（免费）

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel
```

### 6.2 使用 Render/Railway 部署后端（免费）

1. 在 Render 创建 Web Service
2. 连接 GitHub 仓库
3. 配置环境变量
4. 自动部署

### 6.3 使用自己的服务器

```bash
# 1. 服务器上克隆代码
git clone https://github.com/yourusername/lvyuan-agriculture.git
cd lvyuan-agriculture

# 2. 安装依赖并启动
cd backend
npm install
npm start

# 3. 使用 PM2 守护进程
npm install -g pm2
pm2 start server.js --name lvyuan-api
pm2 save
pm2 startup
```

### 6.4 配置 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /var/www/lvyuan;
        index index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://localhost:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 常见问题

### Q1: MongoDB 连接失败？
```bash
# 检查 MongoDB 是否运行
sudo systemctl status mongod

# 启动 MongoDB
sudo systemctl start mongod
```

### Q2: 跨域错误？
修改 `backend/server.js` 中的 CORS 配置：
```javascript
app.use(cors({
  origin: ['http://localhost:8080', 'https://your-domain.com'],
  credentials: true
}));
```

### Q3: 端口被占用？
```bash
# 查找占用 3000 端口的进程
lsof -i :3000

# 杀死进程
kill -9 <PID>
```

---

## 下一步

1. ✅ 本地开发测试
2. ✅ Git 版本管理
3. ✅ 部署到测试服务器
4. ⬜ 配置域名和 HTTPS
5. ⬜ 配置 CDN 加速
6. ⬜ 监控和日志
