# 绿源农业项目 — 零成本部署上线教程

> 本教程手把手教你把项目部署到网上，让任何人都可以通过网址访问。
> 全程免费，不需要买服务器，不需要懂太多技术。

---

## 📋 你需要做的事（总共 7 步）

| 步骤 | 做什么 | 花多长时间 |
|------|--------|-----------|
| 第1步 | 注册 GitHub 账号 | 5 分钟 |
| 第2步 | 注册 MongoDB Atlas 账号（免费云数据库） | 5 分钟 |
| 第3步 | 把代码上传到 GitHub | 5 分钟 |
| 第4步 | 在 Render 部署后端 API | 5 分钟 |
| 第5步 | 在 Vercel 部署前端页面 | 5 分钟 |
| 第6步 | 修改前端 API 地址 | 2 分钟 |
| 第7步 | 初始化数据库 | 2 分钟 |

**总计约 30 分钟**，全程免费。

---

## 第1步：注册 GitHub 账号

GitHub 是全球最大的代码托管平台，免费。

1. 打开 https://github.com
2. 点击右上角 **Sign up**（注册）
3. 填写邮箱、密码、用户名
4. 验证邮箱

> 💡 如果已经有 GitHub 账号，跳过这步。

---

## 第2步：注册 MongoDB Atlas（免费云数据库）

MongoDB Atlas 提供免费的云数据库，不需要自己安装。

1. 打开 https://www.mongodb.com/atlas
2. 点击 **Try Free**（免费试用）
3. 用 Google 或邮箱注册
4. 选择 **Shared**（免费套餐），点击 Create Cluster
5. 设置数据库用户名和密码（**记住这个密码！**后面要用）
6. 在 **Network Access** 页面，点击 **Add IP Address**，选择 **Allow Access from Anywhere**（0.0.0.0/0）
7. 在 **Database** 页面，点击 **Connect** → **Drivers**，复制连接字符串

连接字符串格式如下：
```
mongodb+srv://用户名:密码@cluster0.xxxxx.mongodb.net/lvyuan_farm
```

> ⚠️ **重要**：把上面的"用户名"和"密码"替换成你第5步设置的用户名和密码。这个字符串后面会用到。

---

## 第3步：把代码上传到 GitHub

### 3.1 创建 GitHub 仓库

1. 登录 GitHub
2. 点击右上角 **+** → **New repository**
3. 仓库名填：`lvyuan-agriculture`
4. 选择 **Public**（公开）
5. **不要**勾选 README、.gitignore 等选项
6. 点击 **Create repository**

### 3.2 上传代码

在你的电脑上打开终端（命令行），执行：

```bash
# 进入项目目录
cd /workspace

# 添加远程仓库（把 yourusername 换成你的 GitHub 用户名）
git remote add origin https://github.com/yourusername/lvyuan-agriculture.git

# 推送代码
git push -u origin master
```

> 💡 如果提示输入用户名密码，使用 GitHub 用户名和 Personal Access Token（不是登录密码）。
> 获取 Token：GitHub → Settings → Developer settings → Personal access tokens → Generate new token。

---

## 第4步：在 Render 部署后端 API

Render 是一个免费的后端托管平台。

### 4.1 注册 Render

1. 打开 https://render.com
2. 点击 **Get Started for Free**
3. 用 **GitHub 账号**登录（授权即可）

### 4.2 创建 Web Service

1. 点击 **New** → **Web Service**
2. 连接你的 GitHub 仓库 `lvyuan-agriculture`
3. 配置如下：

| 配置项 | 填写内容 |
|--------|---------|
| Name | `lvyuan-backend` |
| Runtime | `Node` |
| Build Command | `cd backend && npm install` |
| Start Command | `cd backend && npm start` |
| Instance Type | `Free` |

4. 展开下方 **Environment Variables**（环境变量），添加：

| Key | Value |
|-----|-------|
| `PORT` | `10000` |
| `MONGODB_URI` | 第2步复制的 MongoDB 连接字符串 |
| `JWT_SECRET` | 随便写一个长字符串，比如 `my-super-secret-key-2024` |
| `JWT_EXPIRE` | `7d` |
| `NODE_ENV` | `production` |

5. 点击 **Create Web Service**

### 4.3 等待部署

Render 会自动构建和部署，大约需要 2-3 分钟。
看到 **Live** 标签变绿，说明部署成功！

记下你的后端地址，格式类似：
```
https://lvyuan-backend.onrender.com
```

验证：在浏览器打开 `https://lvyuan-backend.onrender.com/api/health`
应该看到 `{"status":"OK","message":"绿源农业API服务正常运行"}`

---

## 第5步：在 Vercel 部署前端页面

Vercel 是一个免费的前端托管平台。

### 5.1 注册 Vercel

1. 打开 https://vercel.com
2. 点击 **Sign Up**
3. 用 **GitHub 账号**登录

### 5.2 部署项目

1. 点击 **Add New** → **Project**
2. 导入你的 GitHub 仓库 `lvyuan-agriculture`
3. 配置如下：

| 配置项 | 填写内容 |
|--------|---------|
| Framework Preset | `Other` |
| Root Directory | `.`（默认） |
| Build Command | （留空） |
| Output Directory | `.`（默认） |

4. 点击 **Deploy**

等待 1-2 分钟，部署完成！

记下你的前端地址，格式类似：
```
https://lvyuan-agriculture.vercel.app
```

---

## 第6步：修改前端 API 地址

现在前端和后端已经分别部署好了，但它们还不知道彼此的地址。

### 方法一：直接修改代码（推荐新手）

1. 在 GitHub 上打开你的仓库
2. 找到并编辑 `js/api.js` 文件
3. 把第5行的地址改成你的 Render 后端地址：

```javascript
// 修改前
const API_BASE_URL = window.__API_URL__ || 'http://localhost:3000/api';

// 修改后（把 lvyuan-backend 换成你的 Render 项目名）
const API_BASE_URL = window.__API_URL__ || 'https://lvyuan-backend.onrender.com/api';
```

4. 提交修改（Commit changes）
5. Vercel 会自动重新部署（等 1 分钟）

### 方法二：在 Vercel 设置环境变量

1. 打开 Vercel 项目设置
2. Settings → Environment Variables
3. 添加：Name = `NEXT_PUBLIC_API_URL`，Value = `https://lvyuan-backend.onrender.com/api`
4. Redeploy

---

## 第7步：初始化数据库

后端部署好后，数据库里还没有数据。需要初始化。

### 方法一：使用 Render Shell（推荐）

1. 打开 Render 控制台
2. 进入你的 `lvyuan-backend` 服务
3. 点击 **Shell** 标签
4. 在命令行输入：
```bash
cd backend && node scripts/seed.js
```

### 方法二：使用 API 调用

在浏览器打开以下地址（把地址换成你的）：
```
https://lvyuan-backend.onrender.com/api/seed
```

看到成功提示后，数据库就初始化完成了。

---

## ✅ 验证部署

1. 打开你的前端地址：`https://lvyuan-agriculture.vercel.app`
2. 页面应该正常显示
3. 点击右上角用户图标，用 `test@example.com` / `test123` 登录
4. 尝试添加商品到购物车

如果一切正常，恭喜你！🎉 你的网站已经上线了！

---

## 📌 部署后的地址汇总

| 服务 | 地址示例 | 用途 |
|------|---------|------|
| 前端 | `https://lvyuan-agriculture.vercel.app` | 用户访问的网址 |
| 后端 API | `https://lvyuan-backend.onrender.com/api` | 数据接口 |
| 数据库 | MongoDB Atlas | 数据存储 |

---

## ❓ 常见问题

### Q: 页面打开了但登录失败？
检查 `js/api.js` 中的 API 地址是否正确，必须包含 `/api` 后缀。

### Q: Render 部署失败？
查看 Render 的 Build Logs，通常是依赖安装失败。确保 `backend/package.json` 正确。

### Q: 免费额度用完了？
- Render Free 会在 15 分钟无请求后休眠，下次请求需要等 30-50 秒启动
- MongoDB Atlas 免费 512MB，够用
- Vercel 免费额度很大，个人项目够用

### Q: 如何更新代码？
1. 本地修改代码
2. `git add . && git commit -m "更新" && git push`
3. Vercel 和 Render 会自动检测并重新部署

### Q: 如何绑定自己的域名？
- Vercel：Settings → Domains → 添加你的域名
- 需要在域名服务商处添加 DNS 记录

---

## 🎯 下一步优化

- [ ] 绑定自己的域名（如 www.lvyuan.com）
- [ ] 配置 HTTPS（Vercel 自动配置）
- [ ] 开启 CDN 加速
- [ ] 配置监控和报警
- [ ] 数据库定期备份
